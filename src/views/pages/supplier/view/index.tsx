import { IconButton } from '@mui/material';

import { ViewIcon } from 'theme/svg';

const SupplierView = () => {
  return (
    <IconButton>
      <ViewIcon />
    </IconButton>
  );
};

export default SupplierView;
